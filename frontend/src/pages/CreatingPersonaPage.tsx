
import clsx from "clsx";
import styles from "./CreatingPersonaPage.module.css";

export default function CreatingPersona() {
    return (
        <h1 className={clsx(styles.title, "fong-onest")}>Creating Persona</h1>
    )
}